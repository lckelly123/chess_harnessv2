---
version: 1
slug: "frontend-index-html"
primary_target: "frontend/index.html"
related_targets: ["frontend/src/App.tsx","frontend/src/styles.css","frontend/src/components/Chessboard.tsx","frontend/src/components/FolderRail.tsx","frontend/src/components/HistoryList.tsx","frontend/src/components/MatchDocket.tsx","frontend/src/components/MatchStatus.tsx","frontend/src/components/ModelSelector.tsx","frontend/src/components/PositionLibrary.tsx","frontend/src/components/PositionQueue.tsx","frontend/src/components/PositionalTesting.tsx","frontend/src/components/positionFilters.ts","frontend/src/components/ReplayControls.tsx","frontend/src/components/RunHistory.tsx","frontend/src/components/TracePanel.tsx"]
---

# Chess research workspace

- Scope: the local React interface in `frontend/`; visitor mode is Operate. Match desk, Match library, and Positional testing are separate destinations in one persistent workspace shell.
- Audience and job: a harness developer watches matches, inspects a selected ply, revisits historical runs, filters the PostgreSQL position library for one turn, or runs a full training/test set and inspects its failures.
- Authority: the user pinned a modern black and purple redesign and delegated the remaining choices. The designer selected the analysis-workbench composition. Code is the system evidence; this pass created no separate concept card or visual comp.
- Direction: near-black graphite surfaces, violet actions and selections, lavender board squares, Archivo text, and one-pixel divisions. Root `DESIGN.md` and `.impeccable/design.json` replace the former cream/green ledger identity.
- First viewport: slim desktop navigation, complete board and replay transport, then selected explanation above compact chronology. New match expands setup; setup opens automatically when no match is selected. Historical browsing has its own library destination.
- Primary actions: choose white and black harnesses and a folder; start or stop a match; replay, scrub, and flip the board; select a trace ply; search, open, and refile records; create folders; filter and select a library position and harness; request one positional move; start a full-set queue or stop after its current position; inspect saved attempts, failures, and model passes.
- Harness choices: Baseline, Agent Player 1, Agent Player 2, and Agent Player 3. Positional testing resolves the four configured versioned IDs against the API and reports missing choices. Shared model controls stay in the shell and lock during a single-position run. Each queue captures its own harness and shared model selection when started; later control changes do not alter it.
- Content: typed harness, model, folder, match, position, player, trace, stored-PGN, one-turn-result, model-run, per-pass, and queue data from the existing FastAPI API. Positional summaries, run history, and queues come from PostgreSQL; dataset `v1` holds 400 positions, split equally between training and test. The component loads position summaries once on mount and reloads on Refresh or Retry. The queen-blunder PGN fixture is removed, with no file fallback. `README.md`, `docs/position-library.md`, and `docs/frontend-backend-contract.md` describe the API and data flow.
- Signature interaction: selecting a trace event selects its ply, synchronizes board and inspector, pauses playback, and enters replay mode. The inspector prefers the completed move event at that ply, then another move event, then the latest event. It presents the recorded explanation and disclosures for metadata and other events at that ply.
- Trace completeness: Moves and All events filters preserve ply groups. Every event remains accessible with timestamp, ply, player, phase, status, summary, stable ID, and recorded detail. Missing explanations have explicit empty states. Internal scrolling brings the selected group into view without moving the page or fighting routine polling.
- Library behavior: folders and search narrow historical matches. Opening a record returns to Match desk for replay. Folder reassignment and pending/error feedback stay local to that row. Harness versions unfold separately from the open action.
- Position library: visible Set, Phase, Position type, and Side to move filters combine locally. More filters contains Dataset, Source, Puzzle theme, and Puzzle rating; search matches opening names, readable themes, and IDs. Results show 12 rows per page with a matching count and Previous/Next controls. Filter changes reset to the first page and select the first match; changing pages selects that page's first row. Rows and the board header show training/test, phase, and quiet/tactical tags. Rows also show side, dataset, available puzzle rating, and source game ID. The board exposes the source link, dataset, last move, rating or unrated status, themes, and a Position details disclosure for ID and FEN.
- Positional behavior: the first returned database position is selected on initial load, leading to the board, four supported harness choices, run command, and persisted attempt. The component stays mounted while navigation hides it, preserving filters, selection, in-flight request, and result. Loading, empty-library import prompt, no matching positions, unavailable, missing-harness, running, error, retry, and completed states are explicit. Refresh retains a selection that still matches the active filters, otherwise selects the first matching row or clears selection when none match. Position-library filters, pagination and refresh, position and harness selections, and shared model controls lock during a single-position run. Success outlines the proposed path on the unchanged board, labels its SAN, and selects the saved attempt in run history when it matches the active run filters.
- Full-set queues: Run a full set sits above the position workspace in the existing graphite/violet system. Position set, Dataset, and Queue harness controls define a new queue alongside the visible shared model. Run full queue shows the full split count (200 for either `v1` set) and ignores library filters. A single run or active queue disables starting another queue; an active queue disables single-run commands while position and trace browsing remain available. Loading, unavailable/retry, and command-error states are explicit.
- Queue progress: Recent queues selects a persisted queue, initially the newest. Queue summaries and the selected detail poll every two seconds. The panel shows captured harness/model/dataset, status, attempted/completed/failed/waiting counts, a progress bar, and the current position. Stop after current lets that position finish, then marks the remaining items skipped. The failure count opens Positions and results with Show failures only selected; its visible chevron rotates with the disclosure. Results stay in a bounded, keyboard-scrollable list with ordinal, phase/type, status, move/classification, and recorded failure stage/message. Position and trace actions select the exercise and move keyboard focus and scroll to the board or exact linked run in Run history.
- Queue persistence: PostgreSQL stores queue membership and item outcomes, linked to the existing model-run traces. Each turn and its evaluation finish before the next begins. Validation, execution, evaluation, and timeout failures are flagged and later positions continue; a move classified as a blunder is a successful execution. A completed queue may include failed items. Browser closure does not stop the worker. Restart retains a saved completed grade, otherwise flags the interrupted attempt and continues pending positions without automatic replay. Runtime details live in `docs/position-library.md#full-set-queues`.
- Run history: the library and board stay above this Operate-mode extension, reached by View run history and inheriting the black/plum/Archivo system. The Position ID filter defaults to the selected exercise; exact Position ID and Run ID filters combine, and All runs clears both. Queue trace actions populate both filters for the linked attempt. Results show 30 runs per page with Previous/Next controls and a selectable list of harness, model, status, run ID, timestamp, and chosen move. Refresh, loading, empty, failure, and retry states are explicit. The list updates every two seconds while a single-position request, full-set queue, or listed run is active; selected running details also poll every two seconds.
- Run inspection: chosen UCI move and Stockfish classification appear above the bounded, keyboard-scrollable model-pass timeline, with centipawn loss when available. Missing classification or CP fields read Not evaluated or No score recorded; mate comparisons can have a classification without CP loss. Every recorded pass exposes phase, status, timestamp, all emitted working notes, and errors; tool disclosures show arguments, results, errors, execution/rollback state, call ID, and available board context. Run details disclose IDs, configuration, and saved evaluation payload. Missing notes, moves, and passes have explicit states. Only emitted observations are presented, never inferred hidden reasoning.
- Constraints: the backend owns state and legal moves. React exposes no human move controls or inferred hidden reasoning. Folders are organizational metadata. A positional response uses the selected database row's exact stored PGN history, creates no match record, and never mutates that history. Ratings, themes, classifications, source metadata, and engine reference answers remain inspection/evaluation data and are excluded from the model's turn input. Detailed match graph execution remains in LangSmith. Positional attempts and passes persist automatically for all four harnesses in `model_runs` and `model_run_passes`. Successful Stockfish grading records the classification, expected-points loss, and strictly better legal-move targets; grading failure preserves the model trace and error. Existing runs are not backfilled.
- Responsive behavior: the desktop positional list is vertically bounded (510px) and scrollable; at 1250px and below its records scroll horizontally with four filter columns; below 1000px navigation moves above content; at 760px and below board/analysis and positional board/controls stack, positional filters return to two columns, and indexes scroll horizontally; below 480px setup, records, and replay reflow further. Run history has a desktop list/detail split, then horizontal run cards above detail at 760px and below. Its pass timeline is bounded at 650px on desktop and 560px on mobile. Explanation and trace regions remain bounded, metadata wraps, and focus stays visible.
- Queue responsiveness: the four-column start form becomes two columns at 1000px and one at 480px. At 760px, headings and stop controls stack, result outcomes move below the position text, and live-position actions wrap. The results list remains bounded at 320px; errors wrap and board/history destinations show visible keyboard focus.
- Motion: brief color feedback, a small explanation entrance, and loading spinners; reduced-motion preference disables transitions and animation.
- Review evidence: final desktop, laptop, library, positional, and three mobile screenshots are under `.impeccable/review/final-*.png`. The parent implementation workflow reported a passing build, lint, eight tests, and an independent rendered finish review. This documentation pass records that evidence; no live model match or positional request was run for the redesign validation.
- Position-library review evidence: `.impeccable/review/position-library-desktop.png` and `.impeccable/review/position-library-mobile.png` record the extension. The implementation workflow reported 328 backend tests including PostgreSQL integration, 13 frontend tests, build and lint, and browser checks for combined filters, pagination, refresh, run locks, and retry. An offline scripted turn against the real database verified exact history and exclusion of inspection metadata from model input; no live model calls were made. Independent finish review closed with a ship disposition after the filtered-selection refresh finding was resolved.
- Run-history review evidence: `.impeccable/review/run-history-desktop.png`, `run-history-mobile.png`, `run-history-desktop-full.png`, and `run-history-mobile-full.png` capture the extension. The implementation workflow reported 338 backend tests including real PostgreSQL, then 28 relevant rechecks after the extraction projection change; 14 frontend tests, build and lint; and one detector pass with no findings. Browser checks covered combined ID filters, notes and tool payloads, failures, empty/retry, polling, pagination, and desktop/mobile overflow. Independent finish review inspected all four captures and returned ship with no material findings. Docker activation preserved 400 positions and 11 matches; no live model calls were made.
- Queue review evidence: `.impeccable/review/queue-desktop.png` and `.impeccable/review/queue-mobile.png` capture the extension. The independent verdict pass scored the disclosure indicator and focus-navigation fixes resolved and returned ship, with no open findings on that fix list.
- Unresolved: match snapshots still use polling and the single-position run POST still waits for a synchronous HTTP response. Run-history GET polling exposes persisted progress during that wait. Full-set queues start asynchronously and run on the backend.
